<div class="row">
  <div class="col m-3">
    <h2 class="text-black">ジャンル編集</h2><br>
    <div class="row">
      <div class="mx-auto">
        <%= form_with model: [:admin, @genre], local: true do |f| %>
          <div><%= f.label :ジャンル名 %></div>
          <div><%= f.text_field :genre_name %></div>
          <div><%= f.submit '変更を保存', class: 'btn btn-success' %></div>
        <% end %>
        <%#= link_to "カートを空にする", cart_items_path, method: :delete, class:"btn btn-danger" %>
      </div>
      </div>
    </div>
  </div>
</div>